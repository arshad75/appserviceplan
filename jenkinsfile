```yaml
stages:
  - build
  - test
  - deploy

jobs:
  build:
    stage: build
    script:
      - npm install
      - npm run build
    artifacts:
      paths:
        - dist

  test:
    stage: test
    script:
      - npm test

  deploy:
    stage: deploy
    script:
      - aws s3 sync dist s3://my-bucket
```