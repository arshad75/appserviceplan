prompt: |
  Generate a complete Jenkins pipeline above code in this repo name the pipeline appropriately, ex if the type is jenkin the name it jenkinsfile. The pipeline should include all necessary stages and steps for a typical Jenkins setup. It should have auth, checkout, build, dockerize and then publish the docker image, only print the pipeline code and do not print any comments