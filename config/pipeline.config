prompt: |
  Generate a complete Jenkins pipeline above python code in this repo. The pipeline should include all necessary stages and steps for a typical Jenkins setup. It should have auth, checkout, build, dockerize and then publish the docker image.